lnpm create vite@latest mandamis -- --template vanilla
cd mandamis
# move the PWA files into this folder (index.html, manifest, sw, icons, app.js)
npm i @capacitor/core @capacitor/cli
npx cap init MANDAMIS com.mandamis.app
npx cap add android
npm run build   # or just keep index.html as root; ensure paths are relative
npx cap copy
npx cap open android  # build signed APK in Android Studio
